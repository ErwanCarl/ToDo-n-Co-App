{% block navbar %}

<nav class="navbar navbar-dark bg-dark" role="navigation">
    <div class="nav-div" id="nav1">
        <ul>
            <li><a href="{{ path('homepage') }}"><i class="bi bi-house pe-2"></i>Accueil</a></li>

            {% if app.user and (is_granted('ROLE_ADMIN') or is_granted('ROLE_SUPER_ADMIN')) %}
            <li><a href="{{ path('user_create') }}"><i class="bi bi-vector-pen pe-2"></i>Créer un utilisateur</a></li>
            <li><a href="{{ path('user_list') }}"><i class="bi bi-file-earmark-person pe-2"></i>Liste des utilisateurs</a></li>
            {% endif %}
        </ul>       
    </div>
    
    <div class="nav-div" id="nav2">
        <ul>
            {% if app.user %}
            <li><a href="{{ path('logout') }}"><i class="bi bi-box-arrow-left pe-2"></i>Déconnexion</a></li>
            {% else %}
            <li><a href="{{ path('login') }}"><i class="bi bi-box-arrow-in-right pe-2"></i>Connexion</a></li>
            {% endif %}
        </ul>        
    </div>  
</nav>

{% endblock %}
